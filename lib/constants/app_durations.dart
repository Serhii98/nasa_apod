abstract class AppDurations {
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
  static const Duration searchDuration = Duration(milliseconds: 500);
  static const Duration expansionDuration = Duration(milliseconds: 200);
  static const Duration textFadeDuration = Duration(milliseconds: 350);
  static const Duration splashDuration = Duration(seconds: 3);
}
